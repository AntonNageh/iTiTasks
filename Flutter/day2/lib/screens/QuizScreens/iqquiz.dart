import 'package:flutter/material.dart';
class IqQuiz extends StatelessWidget {
  const IqQuiz({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(
      title: Text('Page 2'),
    ),
    body: Center(
      child: Text('This is page 2'),
    ),
    );
}
}