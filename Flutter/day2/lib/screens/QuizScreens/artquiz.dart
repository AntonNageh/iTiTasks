import 'package:flutter/material.dart';
class ArtQuiz extends StatelessWidget {
  const ArtQuiz({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(
      title: Text('Page 3'),
    ),
    body: Center(
      child: Text('This is page 3'),
    ),
    );
}
}